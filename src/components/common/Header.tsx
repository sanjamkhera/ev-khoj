// Header.tsx
import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      {/* Content for header like logo, navigation, etc */}
    </header>
  );
};

export default Header;
